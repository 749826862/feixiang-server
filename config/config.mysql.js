module.exports = {
    // 单数据库信息配置
    client: {
      // host
      host: '39.107.252.93',
      // 端口号
      port: '3306',
      // 用户名
      user: 'lingxiang',
      // 密码
      password: '749826862',
      // 数据库名
      database: 'lingxiang',
    },
    // 是否加载到 app 上，默认开启
    app: true,
    // 是否加载到 agent 上，默认关闭
    agent: false,
  };